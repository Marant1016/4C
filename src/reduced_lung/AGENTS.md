# reduced_lung Agent Notes

This directory contains the current reduced-lung model implementation in 4C.

Start future OpenCode sessions by reading `info_oc/reduced_lung_baseline.md`. It documents the baseline/current implementation before the planned thesis changes to the solver workflow.

Unless explicitly requested, do not modify the solver implementation here when updating baseline documentation. The current solver path is NOX-based and is expected to change in future work.
